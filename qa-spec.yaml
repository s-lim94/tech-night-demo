# QA Webshop Audit Specification
# Xsolla Tech Night — May 2026, Kuala Lumpur
# Mode: Browser-driven (works on SPAs)

target: https://webshop-external-demo.xsolla.site/
scope: functional
report_format: markdown
screenshot_dir: ./qa-screenshots

test_areas:
  - homepage loads correctly
  - no images without alt text (a11y)
  - favicon is reachable (HTTP 200)